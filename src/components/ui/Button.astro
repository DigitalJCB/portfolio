---
interface Props {
  variant?: 'primary' | 'secondary' | 'outline';
  href?: string;
  class?: string;
}

const { variant = 'primary', href, class: className = '' } = Astro.props;

const baseClasses =
  'inline-flex items-center justify-center gap-2 px-6 py-3 rounded-full font-semibold transition-all duration-300 hover:scale-105 active:scale-95';

const variantClasses = {
  primary:
    'bg-[#471396] text-white hover:bg-[#5a1db8] shadow-lg shadow-[#471396]/30',
  secondary: 'bg-white/10 text-white hover:bg-white/20 backdrop-blur-sm border border-[#471396]/50',
  outline: 'border-2 border-[#471396] text-white hover:bg-[#471396]/20',
};

const classes = `${baseClasses} ${variantClasses[variant]} ${className}`;

const Tag = href ? 'a' : 'button';
---

<Tag href={href} class={classes}>
  <slot />
</Tag>
