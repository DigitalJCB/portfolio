---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span
  class={`inline-block px-3 py-1 text-sm font-medium bg-white/10 text-gray-300 rounded-full border border-white/10 ${className}`}
>
  <slot />
</span>
